{
  "name": "nama",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "verify": "tsc -p tsconfig.verify.json && node scripts/verify.mjs"
  },
  "dependencies": {
    "@fontsource/archivo-black": "5.3.0",
    "@fontsource/space-mono": "5.3.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.4",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
